# OpenAI (required for LLM steps)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_TEMPERATURE=0.2
# Optional: point to an OpenAI-compatible server (e.g. local Ollama/LM Studio)
# OPENAI_BASE_URL=http://localhost:11434

# NCBI (optional; improves PubMed throughput)
NCBI_API_KEY=
NCBI_TOOL=agentic_systematic_review
NCBI_EMAIL=

# Unpaywall (optional; improves DOI-based OA PDF retrieval)
UNPAYWALL_EMAIL=
